<section class="section pricing" id="pricing">
  <div class="container">
    <div class="section__heading" data-reveal>
      <h2>Pricing Plans</h2>
      <div class="heading-separator">
        <span class="dot"></span>
      </div>
      <p>Curabitur arcu erat, accumsan id imperdiet et, porttitor at sem. Mauris blandit aliquet elit, eget tincidunt.</p>
    </div>

    <div class="pricing__grid">
      <div class="pricing__plan" data-reveal data-reveal-delay="0">
        <div class="pricing__header">
          <h3>Basic</h3>
          <span class="pricing__price">19</span>
        </div>
        <ul class="pricing__features">
          <li>Feature No 1</li>
          <li>Extra Feature</li>
          <li>Extra Feature No 1</li>
          <li>Feature</li>
        </ul>
        <div class="pricing__action">
          <a href="#">Buy Now</a>
        </div>
      </div>

      <div class="pricing__plan" data-reveal data-reveal-delay="100">
        <div class="pricing__header">
          <h3>Premium</h3>
          <span class="pricing__price">29</span>
        </div>
        <ul class="pricing__features">
          <li>Feature No 1</li>
          <li>Extra Feature</li>
          <li>Extra Feature No 1</li>
          <li>Feature</li>
        </ul>
        <div class="pricing__action">
          <a href="#">Buy Now</a>
        </div>
      </div>

      <div class="pricing__plan pricing__plan--popular" data-reveal data-reveal-delay="200">
        <div class="pricing__badge">Popular</div>
        <div class="pricing__header">
          <h3>Pro</h3>
          <span class="pricing__price">39</span>
        </div>
        <ul class="pricing__features">
          <li>Feature No 1</li>
          <li>Extra Feature</li>
          <li>Extra Feature No 1</li>
          <li>Feature</li>
        </ul>
        <div class="pricing__action">
          <a href="#">Buy Now</a>
        </div>
      </div>

      <div class="pricing__plan" data-reveal data-reveal-delay="300">
        <div class="pricing__header">
          <h3>Platinum</h3>
          <span class="pricing__price">45</span>
        </div>
        <ul class="pricing__features">
          <li>Feature No 1</li>
          <li>Extra Feature</li>
          <li>Extra Feature No 1</li>
          <li>Feature</li>
        </ul>
        <div class="pricing__action">
          <a href="#">Buy Now</a>
        </div>
      </div>
    </div>

    <div class="pricing__contact" data-reveal>
      <p>Contact us if you have a special request</p>
      <a href="#contact">Contact Us</a>
    </div>
  </div>
</section>

<style>
  .pricing {
    background: var(--color-surface-alt);
  }

  .pricing__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-md);
    margin-bottom: var(--space-xl);
  }

  .pricing__plan {
    background: var(--color-surface);
    text-align: center;
    border-radius: var(--radius-md);
    overflow: hidden;
    position: relative;
    transition: transform var(--duration-normal) var(--ease-out-expo),
      box-shadow var(--duration-normal) var(--ease-out-expo);
  }

  .pricing__plan:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px oklch(0 0 0 / 0.08);
  }

  .pricing__plan--popular {
    border: 2px solid var(--color-brand);
    transform: scale(1.05);
  }

  .pricing__plan--popular:hover {
    transform: scale(1.05) translateY(-4px);
  }

  .pricing__badge {
    position: absolute;
    top: var(--space-sm);
    right: var(--space-sm);
    background: var(--color-brand);
    color: var(--color-elevated);
    padding: 4px 12px;
    font-size: var(--font-size-xs);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: var(--tracking-wider);
    border-radius: var(--radius-full);
  }

  .pricing__header {
    padding: var(--space-lg) 0;
    border-top: 2px solid var(--color-brand);
    border-bottom: 2px solid var(--color-brand);
  }

  .pricing__header h3 {
    font-size: var(--font-size-xl);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    margin-bottom: var(--space-sm);
  }

  .pricing__price {
    font-size: var(--font-size-5xl);
    font-weight: 800;
    position: relative;
  }

  .pricing__price::before {
    content: '$';
    font-size: var(--font-size-2xl);
    position: relative;
    top: -0.4em;
    margin-right: 4px;
    font-weight: 600;
  }

  .pricing__price::after {
    content: '/mo';
    font-size: var(--font-size-base);
    font-weight: 500;
    color: var(--color-ink-muted);
    margin-left: 4px;
  }

  .pricing__features {
    padding: var(--space-md) 0;
    border-bottom: 2px solid var(--color-brand);
  }

  .pricing__features li {
    padding: var(--space-sm) 0;
    font-size: var(--font-size-base);
    color: var(--color-ink-soft);
    position: relative;
  }

  .pricing__features li:not(:last-child)::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 140px;
    height: 1px;
    background: var(--color-brand-light);
  }

  .pricing__action {
    padding: var(--space-lg) 0;
  }

  .pricing__action a {
    display: inline-block;
    padding: var(--space-sm) var(--space-lg);
    border: 2px solid var(--color-brand);
    color: var(--color-brand);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    font-size: var(--font-size-sm);
    border-radius: var(--radius-sm);
    transition: all var(--duration-fast) var(--ease-out-expo);
  }

  .pricing__action a:hover {
    background: var(--color-brand);
    color: var(--color-elevated);
  }

  .pricing__contact {
    text-align: center;
  }

  .pricing__contact p {
    font-size: var(--font-size-xl);
    font-weight: 500;
    margin-bottom: var(--space-md);
    color: var(--color-ink-soft);
  }

  .pricing__contact a {
    display: inline-block;
    padding: var(--space-sm) var(--space-lg);
    background: var(--color-brand);
    color: var(--color-elevated);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    font-size: var(--font-size-sm);
    border-radius: var(--radius-sm);
    transition: background var(--duration-fast) var(--ease-out-expo),
      transform var(--duration-fast) var(--ease-out-expo);
  }

  .pricing__contact a:hover {
    background: var(--color-brand-dark);
    transform: translateY(-2px);
  }

  @media (max-width: 1024px) {
    .pricing__grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .pricing__plan--popular {
      transform: none;
    }
    .pricing__plan--popular:hover {
      transform: translateY(-4px);
    }
  }

  @media (max-width: 600px) {
    .pricing__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
